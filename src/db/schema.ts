// Data model is not yet specified (no spec/plan exist for tank builds,
// parts, or match history yet -- see status.md). This file is a
// placeholder so drizzle.config.ts and the db client have a schema
// module to point at; tables land once /speckit-plan produces the ADR
// for the persistence pattern.
export {};
